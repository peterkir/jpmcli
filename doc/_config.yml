plugins:
  - jekyll-github-metadata
  - jekyll-seo-tag
  - jekyll-sitemap
markdown: kramdown

github: [metadata]
repository: peterkir/jpmcli

defaults:
  -
    scope:
      path: ""
    values:
      layout: default
